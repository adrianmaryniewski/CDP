describe('test description', () => {

    // create beforeEach method
    beforeEach(() => {
        cy.clearLocalStorage()
    })

    it('should be able to setup filters', () => {

    })
})

// check github account in the terminal

// You can check your GitHub account in the terminal by using the git command. For example, you can use the command "git config --list" to view your GitHub account information.